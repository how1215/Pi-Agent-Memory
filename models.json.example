{
  "_comment": "Copy this file to ~/.pi/agent/models.json and replace the model ID with one installed in Ollama.",
  "providers": {
    "ollama": {
      "baseUrl": "http://localhost:11434/v1",
      "api": "openai-completions",
      "apiKey": "ollama",
      "models": [
        { "id": "qwen2.5-coder:7b", "input": ["text"] }
      ]
    }
  }
}
